description: The definition of a plant in the PlantDictionary
type: object
x-examples:
  example-1:
    name: Cabbage
    description: 'The Cabbage is a mundane plant that grows above ground. It is relatively fast growing and easy to care for, not requiring any additional watering in most climates'
    growth_stages: []
examples:
  - name: string
    description: string
    growth_stages:
      - name: string
        description: string
        action: string
        optional: true
        action_to_skip: string
        consumable_options:
          - name: string
            quantity: 0
        added_yield: 0
        growth_time: 0
        harvestable:
          produce: {}
          seeds: {}
          goods: {}
          final_harvest: true
title: ''
properties:
  name:
    type: string
    minLength: 1
  description:
    type: string
    minLength: 1
  min_size:
    $ref: ./Size.yaml
  max_size:
    $ref: ./Size.yaml
  growth_stages:
    type: array
    items:
      $ref: ./Growth-Stage.yaml
required:
  - name
  - description
  - min_size
  - max_size
  - growth_stages
